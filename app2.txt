import pickle

# Path to your .pkl file
filename = 'LinearSVCTuned.pkl'  # change this to your actual filename

# Open and load the file
with open(filename, 'rb') as file:
    data = pickle.load(file)

# Now `data` contains your loaded object (e.g., model, vectorizer, etc.)
print(type(data))
